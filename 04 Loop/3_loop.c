#include <stdio.h>

int main() {
    int n;

    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        printf("%d ", (i % 2) ? 1 : 0); // Brooo it's Ternary Operator :), Be unique OKAYYYYYYYYYYYY; Sir just 1 time said about this, don't Worry
    }

    return 0;
}